# led-remote 프로젝트 메모

## 프로젝트 개요
- iPixel Color 앱으로 제어하는 **블루투스 LED 매트릭스 패널**(플렉서블, 풀컬러)을 직접 만든 웹페이지로 제어하는 프로젝트.
- `index.html` 한 파일에 HTML/CSS/JS가 모두 들어 있음. 외부 빌드 도구 없음.
- 사용자는 코딩 경험이 거의 없음. 설명은 쉬운 한국어로, 단계별로.
- 배포: GitHub Pages → https://gilbertgo.github.io/led-remote/ (안드로이드 크롬에서 사용)
- 로컬 개발: Windows PC, VS Code + Live Server (`http://127.0.0.1:5500/index.html`), 크롬. PC 인텔 블루투스로 연결 테스트.
- GitHub 저장소: GilbertGo/led-remote (public, main 브랜치). 로컬 경로 `C:\GitHub\led-remote`

## 하드웨어
- 패널 이름: `LED_BLE_707D84B3` (이름이 `LED_BLE`로 시작)
- **96×16 픽셀 확정** (2026-09-25, 96×16 이미지 테두리가 패널 가장자리 네 변에 딱 맞음).
- ESP32 DevKit(ESP-32D, CH340C)도 있지만 **현재 패널과는 무관**. 패널은 자체 컨트롤러로 동작. 나중에 ESP32가 BLE로 패널에 명령을 보내는 확장 가능성 있음.

## BLE 통신 (Web Bluetooth)
- 서비스 UUID가 OS마다 다르게 보임:
  - 안드로이드: `0000fa00-0000-1000-8000-00805f9b34fb`
  - Windows 크롬: `000000fa-0000-1000-8000-00805f9b34fb` (바이트가 뒤집혀 보임)
  - 그래서 두 UUID를 모두 optionalServices에 넣고 순서대로 시도함.
- 특성(characteristic)은 UUID 대신 속성으로 찾음: write 가능한 것(uuid에 fa02/02fa 우선) = 쓰기, notify 가능한 것 = 응답.
- 기타 서비스: `0000ae00`, `00001800` (사용 안 함)
- 한 번에 한 기기만 연결 가능. 테스트 시 폰의 iPixel Color 앱/페이지는 닫아야 함.

## 확인된 명령 (형식: [길이L, 길이H, 명령L, 명령H, 데이터...])
| 기능 | 바이트 | 확인 |
|---|---|---|
| 전원 켜기/끄기 | `05 00 07 01 01` / `05 00 07 01 00` | ✅ 동작 |
| 밝기(1~100) | `05 00 04 80 <값>` | ✅ 동작 |
| 그리기(DIY) 모드 켜기/끄기 | `05 00 04 01 01` / `05 00 04 01 00` | ✅ |
| 점 찍기 (방식 A, 기본) | `0a 00 05 01 00 R G B X Y` | ✅ 동작 |
| 점 찍기 (방식 B, 예비) | `0a 00 05 01 R G B ff X Y` | 미확인 |
| 이미지(PNG) 한 번에 보내기 | 아래 "이미지 보내기" 참고 | ✅ 동작 (응답 `05 00 02 00 03`) |
| 글자 보내기(흐르기 등) | 아래 "글자 보내기" 참고 | ✅ 동작 |

- 패널 응답(알림): 받은 명령 번호를 되돌려주고 끝에 `01` (예: 밝기 → `05 00 04 80 01`, 전원 → `05 00 07 01 01`).
- 점 찍기 응답: iPixel-CLI는 점 찍기에 응답을 기다리지 않음(`requires_ack=False`). 페이지는 연결 후 첫 점으로 응답 여부를 자동 확인해, 없으면 `PIXEL_GAP`(15ms) 간격 전송으로 바꿈.

### 이미지 보내기: 화면 전체를 한 번에 (iPixel-CLI `send_image.py` 방식)
- 2026-09-25 구현(`sendImageJob`, 버튼 "패널로 한 번에 보내기"). **실제 패널에서 동작 확인** (PC 크롬): 96×16 테두리 그림 → PNG 123바이트, 244바이트 1회 전송, 응답 `05 00 02 00 03`(완료), 0.4초. 패널에 테두리가 한 번에 표시됨.
- PNG는 캔버스 toBlob이 아니라 직접 만듦(RGB 8비트, 투명도 없음, `CompressionStream('deflate')`로 압축) — iPixel-CLI(PIL RGB PNG)와 같은 형식.
- 점이 아니라 **PNG 파일 바이트 그대로** 보냄 (GIF도 가능, 헤더 첫 바이트만 `03`).
- 메시지 = `[길이L, 길이H]` + `02 00 <옵션>` + `<PNG 크기 4바이트 LE>` + `<CRC32 4바이트 LE>` + `00 <슬롯>` + PNG 바이트
  - 길이 = (뒤따르는 전체 바이트 수 + 2), 2바이트 little-endian
  - 옵션: 첫 12KB 묶음 `00`, 그다음 묶음 `02` (96×16 PNG는 보통 12KB 미만이라 한 묶음)
  - 슬롯: `00` = 바로 표시만(저장 안 함), 1 이상 = 해당 슬롯에 저장
- 메시지를 **244바이트씩 잘라** writeWithResponse로 연속 전송 → 알림 `05 .. .. .. 00/01`(묶음 받음), `05 .. .. .. 03`(전체 완료)을 기다림 (최대 8초).

### 글자 보내기: 패널 자체 글자 명령 (iPixel-CLI `send_text`, 2026-09-25 구현, **실기기에서 동작 확인** ✅ PC 크롬)
- 이미지와 같은 전송 형식(`sendFramed`)인데 머리가 `02 00` 대신 **`00 01`**.
- 데이터 = `[글자 수(1바이트, 최대 255)]` + 설정 13바이트 `00 01 01 <움직임> <빠르기 0~100> <무지개 0~9> R G B 00 00 00 00`(배경색 없음) + 글자 블록들
  - 움직임: 0 가만히, 1 왼쪽 흐르기, 2 오른쪽 흐르기(글자 순서를 거꾸로 보냄), 5 깜빡임, 6 숨쉬기, 7 눈송이 (3·4 위/아래는 32×32 전용)
  - 글자 블록 = `<00=8칸 폭, 01=16칸 폭>` R G B + 16줄 점 그림 (한 줄에 8칸당 1바이트, **왼쪽 칸 = 가장 낮은 비트**)
- 글자 모양은 브라우저 캔버스에 시스템 글꼴(맑은 고딕 등)로 그려 투명도 100 기준으로 흑백 변환. 한글은 16칸, 영문은 8칸(조금 넓으면 옆으로 줄임, W·M처럼 아주 넓으면 16칸).
- 안 되면 대안: "그림판에 옮기기" → 이미지 보내기(검증된 방식, 대신 흐르기 없음).

### 조사: 패널 정보 명령 (가로 크기 확인용, 미확인)
- `08 00 01 80 <시> <분> <초> 00` → 응답의 5번째 바이트(d[4])가 기기 종류. `0x84`(132) = **96×16**, `0x83` = 64×16, `0x87` = 144×16, `0x88` = 192×16.

- ⛔ `04 00 03 80` = 저장된 내용·설정 전체 삭제(iPixel-CLI `clear`). 절대 보내지 말 것.
- 참고 자료: github.com/lucagoc/iPixel-CLI, github.com/cagcoach/ha-ipixel-color (프로토콜 문서). 비공식 역분석 자료라 일부 부정확할 수 있음.
- ⚠️ EEPROM 삭제 등 위험 명령은 기기를 부팅 루프에 빠뜨릴 수 있다고 알려져 있음. 사용하지 말 것.

## 현재 상태
- 1단계(연결, 전원, 밝기): 완료, 폰/PC 모두 동작.
- 2단계(그림판): 동작함. 2026-09-25 개선:
  - 점 찍기: writeValueWithResponse + 점 사이 간격(`PIXEL_GAP`, 화면 슬라이더 0~40ms). 빨라짐 확인. 최적 간격 값은 미정.
  - 선 끊김: pointermove 사이 칸을 브레젠험 직선으로 채움 (실기기 확인은 따로 안 받음).
  - 이미지 한 번에 보내기: 동작 확인 ✅ (훨씬 빠름).

## 다음 할 일
1. 3단계(글자 보내기) 완료·동작 확인. 남은 확인: 움직임 종류별 동작, 폰(안드로이드)에서 한글 모양.
2. ESP32 시계·날씨 동작 확인 ✅. 남은 것: 방 안 온습도 센서 추가, (선택) 같은 이름 와이파이 중 가장 센 쪽에 붙기.
3. 이후 아이디어: 그림판을 그릴 때 자동으로 이미지 전송(점 찍기 대체).

## ESP32 시계·날씨 (`esp32/led_clock/`, 2026-09-25, **실기기에서 시계·날씨 동작 확인** ✅)
- 동작: 와이파이 → 인터넷 시간(KST) + Open-Meteo 날씨(무료, 키 없음, http, 인천시청 좌표) → 블루투스로 패널에 번갈아 전송 (시계 30초, 날씨 10초, 날씨는 10분마다 갱신)
  - 시계 ✅: 패널 자체 시계(시간 → 요일·날짜를 패널이 알아서 번갈아 보여줌). 시간 맞추기 `08 00 01 80 시 분 초 00` (응답 `0b 00 01 80 84 …`, 84 = 96×16 기기) + 시계 모드 `0b 00 06 01 <모양0~8> <24시간1> <날짜표시> <년%100> <월> <일> <요일 월=1…일=7>` (응답 `05 00 06 01 01`)
  - 날씨 ✅: **패널 글자 명령**으로 보냄 — [날씨 그림 16칸(한 색)] [기온 주황] [습도 파랑]. 숫자·기호·한글 모양은 웹페이지 `renderGlyph`로 뽑아 코드에 넣은 것.
  - ❌ 압축 안 한(stored deflate) PNG 이미지는 패널이 `03`(완료) 응답은 하지만 화면이 꺼짐 → 쓰지 말 것. (웹페이지의 압축 PNG는 됨)
  - 상태 메시지: 문제 생기면 패널에 흐르는 글자로 표시 — 패널 연결 성공 / 와이파이 연결 중 / 와이파이 안 됨 / 시간 못 받음 / 날씨 못 받음 (같은 메시지는 반복 전송 안 함)
  - 블루투스는 **보낼 때만** `NimBLEDevice::init` → 연결 → 전송 → 끊기 → `deinit`. 그 사이 폰·웹페이지로 패널 연결 가능.
- ⚠️ 와이파이 신호가 핵심: 패널 방에서 `U+Net48F0` 신호 -73~-75면 와이파이 연결은 돼도 인터넷(시간·날씨)이 실패함. 원인은 TP-Link 와이파이 중계기 위치였고, 옮긴 뒤 해결. 끊김 이유 코드 2·4·15·201·36은 약한 신호에서도 나옴(비밀번호 문제 아님).
- 방 안 온습도: 센서 아직 없음 → 나중에 추가 (추천: AHT20 또는 SHT31, I2C)
- 와이파이 정보: `secrets.h` (gitignore됨, `secrets.example.h` 복사해서 작성). 2.4GHz만 가능 (`U+Net48F0` O, `U+Net48F0_5G` X).
- 도구: arduino-cli (`C:\Program Files\Arduino CLI\arduino-cli.exe`, winget 설치), esp32 코어 3.3.12, NimBLE-Arduino 2.5.1, ArduinoJson 7.4.3
- 컴파일: `arduino-cli compile --fqbn esp32:esp32:esp32:PartitionScheme=huge_app --output-dir build .` (1.2MB라 기본 파티션 초과)
- 업로드 (이 보드는 자동 받기 모드가 안 됨, COM5, CH340):
  1. 사용자가 BOOT 누른 채 EN 눌렀다 떼고 BOOT 떼기 → "됐어"
  2. 그다음에 실행: `esptool.exe --chip esp32 --port COM5 --baud 460800 --before no-reset --after no-reset write-flash 0x0 build/led_clock.ino.merged.bin` (esptool: `%LOCALAPPDATA%\Arduino15\packages\esp32\tools\esptool_py\5.3.1\`)
  3. 사용자가 EN만 눌러 재시작. 프로그램이 돌고 있는 중에 esptool을 먼저 켜 두면 "No more data"/"Invalid head of packet"로 실패함.
- 시리얼 기록: 115200, DTR/RTS 끈 채로 읽기 (PowerShell SerialPort).

## 작업 규칙
- 변경 후 사용자가 Live Server 페이지에서 직접 테스트함. 무엇을 확인하면 되는지 짧게 안내할 것.
- 통신 기록(log) 영역은 디버깅에 중요하니 유지.
- GitHub에 올리는 건 사용자가 요청할 때만.
